services:
  echo:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - echo_data:/data
    environment:
      - ECHO_DB=/data/echo.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 3

volumes:
  echo_data:
